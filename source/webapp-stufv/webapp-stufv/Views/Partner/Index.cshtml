@section content {
    <div class="container">
        <div class="page-header">
            <div class="well">
                <h1>Partners</h1>
            </div>
        </div>
        @foreach (var item in ViewBag.Partner)
        {
            @item.Name
        }
    </div>

}
